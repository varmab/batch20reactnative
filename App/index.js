import React from 'react';
import {
    SafeAreaView
} from 'react-native'

import CurrentList from './screens/CurrentList'

export default ()=>{
    return (
        <SafeAreaView>
            <CurrentList/>
        </SafeAreaView>
    )
};